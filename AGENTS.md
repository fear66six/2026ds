# 2026 电赛 E 题项目规则

## 适用范围与事实来源

本文件适用于项目根目录及全部子目录。项目中的实际源码、固件源码和 `docs/` 原始资料是主要事实来源；聊天描述、旧整理文档、AI 总结和本文件中的示例关键词只可作为检索线索。

事实来源优先级：

1. 实际源码、固件源码、配置和构建文件：确认函数、签名、编码、字段顺序、校验、默认值、超时、重试和实际程序行为。
2. 对应厂商正式资料：确认型号、接口、引脚、版本说明、物理单位、电气参数、环境和限制。
3. 厂商示例或教程：只确认该示例版本的用法。
4. 用户项目文档：确认已作出的工程决定、测试结果和项目结构；与原始源码或厂商资料冲突时必须并列记录。
5. 旧整理、AI 总结和聊天描述：不得单独支撑硬件型号、版本、API、单位、协议、接线、寄存器或安全范围。

必须严格区分亚博智能 Yahboom Jetson、幻尔科技 Hiwonder NexArm、ST STM32F103C8T6、NVIDIA 官方 Jetson、K230/WonderMK/WonderMV、用户工程和第三方项目。不得跨设备移植 API、引脚或协议结论。

## 原始资料优先

任何涉及硬件型号、固件版本、接线、GPIO、串口、USB、摄像头、NexArm API/协议、STM32、电磁铁、MOSFET、继电器、OpenCV、标定、机械坐标或安全范围的任务，必须先搜索项目文件，不得仅凭模型知识、旧总结或聊天描述直接实现。

源码不能自动证明真实物理单位、当前实机固件、机械安全范围、当前接线、设备节点、实际方向或精度。示例中的 `/dev/ttyUSB0`、IP、GPIO 或路径均不是当前设备事实。

## 证据与结论格式

每个重要结论至少记录：

- 来源相对路径；
- PDF 页码、章节或代码符号/行号；
- 来源类型；
- 可信等级；
- 是否需要实机验证。

可信等级统一为：

- A：源码直接确认；
- B：对应厂商正式资料；
- C：厂商示例或教程；
- D：用户项目决策或工程建议；
- U：无法确认。

先列证据，再下结论。存在冲突时列出双方来源，不得静默覆盖。工程推断、项目决策和待实机验证必须与来源确认事实分开。

## PDF 处理

1. 先逐页提取文本并保留页码对应关系。
2. 页面主要为截图、引脚图、接线图、表格、原理图、寄存器图或 Jetson-IO 界面时，必须渲染相关页做视觉检查。
3. 文本提取为空不等于 PDF 没有信息。
4. 不对全部 PDF 无差别 OCR；仅在相关页文本不可读时局部 OCR 或人工查看。
5. 图像、表格和正文冲突时记录冲突。
6. `.cache/docs_index/pdf_text/` 只是指向原 PDF 的逐页缓存，不是独立事实文件。

## ZIP 处理

- 只在临时目录、缓存目录或内存中读取，不修改原 ZIP。
- 记录内部目录树、关键源码、固件/文档、版本候选和关键文件 SHA256。
- 检查重复内容以及同名不同哈希文件。
- 内部路径必须可追溯为 `原ZIP路径!/内部路径`。
- 不运行内部 Demo，不安装内部依赖，不永久保存大型完整解压副本。

## 源码处理

Python：

- 使用 AST 或静态阅读提取模块、类、函数、方法、完整签名、默认值和定义行。
- 不 import 可能自动访问硬件的项目/厂商模块，不运行厂商 Demo。
- 示例调用不能证明不存在于源码中的 API。

C/C++/Arduino：

- 静态提取宏、CMD、enum、struct、协议字段、函数声明和函数定义。
- 区分声明与实现，区分固件能力与上位机 SDK 已暴露能力。
- 数值类型（如 `int16`）不能自动解释为毫米、角度或其他物理单位。

## 版本判断

1. 先匹配当前硬件型号。
2. 仅在适配当前硬件的资料中优先较高版本。
3. 版本相同或缺失时再参考日期。
4. 日期最新但硬件不匹配的资料不能作为主结论。
5. 历史版本保留在索引中。
6. 无法判断时列出全部候选、证据和冲突；近期购买不是具体版本证据。

## 每个开发任务的标准流程

开始前：

1. 明确任务涉及的设备、厂商和边界。
2. 运行 `python tools/search_docs.py "关键词"` 搜索索引。
3. 打开命中的原始 PDF、源码及 ZIP 内关键文件；缓存只负责定位，不能替代原件。
4. 检查相关图片、表格、原理图或界面页。
5. 输出“本次使用的事实来源”，列出实际路径和页码/符号。
6. 分开标注：来源确认事实、工程推断、项目决策、待实机验证。
7. 之后才开始设计或编码；资料不支持的内容写成工程建议或待确认。

完成后：

1. 不把完整 API、协议或参数再次复制到统一总结。
2. 仅在长期事实、工程决策或实机待确认项变化时，分别更新 `docs/PROJECT_FACTS.md`、`docs/DECISIONS.md`、`docs/TODO_VERIFY.md`。
3. 新增资料、源码或固件变化后运行 `python tools/update_docs_index.py`，更新哈希与符号索引。
4. 不因旧缓存存在而跳过原始资料核查。

## 安全限制

未经用户明确批准，不得：

- 打开真实串口或访问 `/dev/tty*`；
- 建立 NexArm Socket 连接；
- 操作 Jetson GPIO、执行 `busybox devmem`；
- 修改设备树或使用 Jetson-IO 写入配置；
- 烧录 STM32、ESP32、AT32；
- 运行机械臂运动示例或发送运动命令；
- 控制继电器、MOSFET或给电磁铁通电；
- 安装系统软件、驱动或厂商 Demo 依赖。

任何真实硬件代码必须：

- 默认禁用硬件访问；
- import 时不连接，构造函数中不自动连接；
- 不得用单一开关直接启用运动；
- 提供 Mock 模式；
- 具有异常、超时和进程退出时的安全关闭逻辑。

允许的默认操作仅限文件扫描、哈希、PDF 文本提取/局部渲染、ZIP 静态读取、源码/AST 分析、Markdown 链接检查和不访问硬件的语法检查。

## 索引维护

- `.cache/docs_index/` 是可随时删除重建的机器缓存，不是事实来源。
- `tools/update_docs_index.py` 不得修改原始资料、运行示例或访问硬件。
- `tools/search_docs.py` 只用于定位相关原件、PDF 页码、代码符号和 ZIP 内路径。
- 旧整理结果保留并在 `docs/README.md` 标为历史参考，不作为最高可信来源；不得再创建大型“统一整理版”。
