# 开源发布检查清单

本清单面向首次将仓库设为公开可见。它不改变源码，也不替代许可证或第三方合规审核。

## 发布前必须完成

- [x] **选择开源许可证**：原创代码和文档采用 MIT License，版权人标注为 `fear66six`。
- [ ] **确认第三方再分发条件**：重点检查 `firmware/**/cmsis/` 中 ARM/ST 文件、NexArm 厂商 SDK，以及 `TaskSuite_E/` 的来源与授权。
- [ ] **补充本人贡献范围**：在 `docs/PORTFOLIO.md` 中只写真实负责的模块，区分团队成果与个人贡献。
- [ ] **补充奖项佐证**：如适合公开，可在 `assets/showcase/` 放置脱敏证书或官方结果链接；否则保留“项目负责人提供”的说明。
- [x] **检查当前快照中的隐私与设备标识**：未发现常见私钥、访问令牌或包含个人用户名的本机路径；已排除原始日志。仓库保留 Jetson 默认 USB 网络地址和本项目串口 `by-id` 作为可追溯部署样例，它们不是访问凭据，使用者必须按自己的设备替换。
- [x] **清理已追踪生成物**：已从当前 Git 快照移除 `runs/`、`output/`、`logs/`、`tmp/`、`backup/` 和固件构建日志，并用仓库检查脚本防止重新纳入。
- [ ] **确认历史记录**：即使当前文件已删除，Git 历史仍可能包含设备信息或第三方文件；必要时在公开前重写历史。

## 建议补充

- [ ] 30–60 秒现场演示视频或 GIF；
- [ ] 一张完整装置照片和一张系统连线示意图；
- [ ] 团队成员与分工（获得所有成员同意后公开）；
- [ ] GitHub Topics，例如 `computer-vision`、`robotics`、`embedded-systems`、`jetson`、`stm32`；
- [x] Issue / Pull Request 模板和持续集成；
- [ ] 在仓库 About 中填写一句话介绍、演示链接和项目主页。

## 当前已检查

- [x] `pintu/` 已被忽略并明确为只读外部参考；
- [x] README 已将展示入口与内部证据文档分开；
- [x] README 中的获奖表述注明信息来源；
- [x] 已用 `THIRD_PARTY_NOTICES.md` 明确根 MIT License 的第三方例外；
- [x] 已将无项目修改的 ARM/ST CMSIS 与启动文件从 Git 索引移除，并保留构建依赖说明；
- [x] 自动测试发现范围不包含显式硬件脚本；
- [x] 展示图片复制到稳定路径，README 不依赖时间戳运行目录；
- [x] 公开文档明确区分软件流程完成与实机效果确认。
- [x] 当前追踪文件中未发现常见私钥、GitHub Token 或 AWS Access Key 模式；
- [x] 当前追踪文件中没有超过 1 MiB 的大文件。
