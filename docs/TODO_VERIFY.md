# 必须由实机或现场信息确认的事项

以下事项已经搜索原始 PDF、源码、ZIP 清单和相关图页；不是“尚未搜索”的占位项。

## V-001 当前 Jetson 模组与载板的精确型号

- 问题：当前实物是何种 Jetson Orin 模组、官方载板还是亚博/SUB 载板，是否属于 SUPER 配置？
- 已搜索范围：`docs/板端/01 第一章 快速上手/`、`docs/板端/02 第二章 主板基础/`、`docs/板端/04 第四章 GPIO控制/`，并视觉核查 `主板介绍.pdf` 第 1 页和 `GPIO说明.pdf` 第 2 页。
- 当前候选：资料同时覆盖 Jetson Orin 官方套件、SUB 套件和 SUPER 相关流程。
- 无法确认原因：资料集合描述多个产品/流程，没有当前实物铭牌或系统身份输出。
- 确认方式：检查模组和载板丝印/订单；在获准后记录只读系统信息（如 JetPack/L4T、设备树 model），再与对应厂商资料匹配。
- 是否涉及硬件风险：低；错误结论用于 GPIO/刷机时风险高。
- 是否阻止当前开发：阻止板级 GPIO、设备树和系统镜像相关实现；不阻止纯 Mock 软件设计。
- 优先级：高。

## V-002 当前 JetPack/L4T 与 GPIO 配置状态

- 问题：实机 JetPack、L4T、内核、GPIO 库和 pinmux/设备树的实际版本与状态是什么？
- 已搜索范围：`docs/板端/02 第二章 主板基础/` 和 `docs/板端/04 第四章 GPIO控制/` 全部 PDF 的逐页索引，相关 GPIO 图页已视觉检查。
- 当前候选：资料包含多个系统烧录、升级和 SUPER 修复流程。
- 无法确认原因：软件版本和 pinmux 是实机可变状态，教程版本不能证明当前状态。
- 确认方式：用户批准后执行只读版本/设备树检查；任何 Jetson-IO、`devmem` 或写入操作需另行明确批准。
- 是否涉及硬件风险：只读检查低，配置写入高。
- 是否阻止当前开发：阻止真实 GPIO 映射和驱动部署；不阻止接口抽象与 Mock。
- 优先级：高。

## V-003 当前 NexArm 控制板和固件版本

- 问题：当前机械臂控制板硬件版本、角色和实机固件版本是什么？
- 已搜索范围：NexArm 快速入门、外部主控二次开发 PDF/SDK、`SCH_NexArm V1.0.pdf` 图页、出厂固件目录和 ZIP 清单。
- 当前候选：V1.0 NexArm-ESP32 控制器图纸；资料包包含 follower V1.2 和 leader V1.1 固件文件。
- 无法确认原因：图纸/固件包的存在不证明实机硬件或当前已烧录版本。
- 确认方式：先看控制板丝印/标签；经用户明确批准并确保不运动后，使用 SDK 的只读固件查询或厂商工具读取。
- 是否涉及硬件风险：中；连接或误发命令可能触发机械动作。
- 是否阻止当前开发：阻止真实连接、固件选择和运动测试；不阻止协议静态分析。
- 优先级：高。

## V-004 NexArm 当前通信链路和端点

- 问题：项目最终使用 UART 还是 Wi-Fi，实际串口节点或主机地址/端口是什么？
- 已搜索范围：外部主控二次开发 PDF、UART/Wi-Fi SDK 与 Demo、ZIP 内部路径、Jetson 远程连接教程。
- 当前候选：SDK 同时提供 UART `NexArmClient` 和 Wi-Fi `NexArmWiFiClient`；源码中存在示例默认值。
- 无法确认原因：端点取决于当前接线、系统枚举、网络配置和项目主动选择。
- 确认方式：先作架构决策；经批准后只读枚举串口/网络，再进行不运动的握手验证。
- 是否涉及硬件风险：中。
- 是否阻止当前开发：阻止真实传输适配器启用；不阻止统一接口和 Mock。
- 优先级：高。

## V-005 当前 MOSFET 模块的输入极性与 3.3 V 兼容性

- 问题：实际 15 A MOSFET 模块是否确为高电平开启、低电平关闭，PB12 的 3.3 V 高电平是否能可靠驱动，输入端是否自带上拉/下拉或反相级？
- 已搜索范围：`docs/MOSFET驱动15A资料包/` 的产品资料、接线图片、ZIP 目录和 STM32 示例源码；示例只确认用 PC13 输出控制，没有形成当前模块输入阈值和极性的可靠厂商结论。
- 当前候选：项目已主动决定按 PB12 高开、低关实现；原始资料未充分证明实际模块的输入电气特性。
- 无法确认原因：输入阈值、默认状态和极性取决于手中模块版本，必须通过板上丝印/原理图或空载测量确认。
- 确认方式：保持 MOSFET 输出端、电磁铁和负载电源断开，先核对模块型号与输入原理图；之后按 `PB12_NO_LOAD_TEST_GUIDE.md` 分阶段测量 PB12 和模块控制端。
- 是否涉及硬件风险：高；极性或阈值判断错误可能导致上电自动吸合。
- 是否阻止当前开发：不阻止固件静态实现和编译；阻止连接 MOSFET、连接电磁铁和任何负载测试。
- 优先级：最高。

## V-006 电磁铁精确型号、额定电压和线圈参数

- 问题：实际使用 P20/15、ZHI-1727 或其他型号，额定电压、电阻、电流和允许通电周期是多少？
- 已搜索范围：`docs/MOSFET驱动15A资料包/资料/` 两份产品手册、`接线方式/` 图片、三个示例 ZIP 目录与源码。
- 当前候选：P20/15 和 ZHI-1727。`P20_15_5V电磁铁产品手册.pdf` 文件名含“5V”，但第 1 页产品照片铭牌可见“DC12V”，形成明确冲突。
- 无法确认原因：资料包包含多型号且文件名与图页不一致，不能确定到手器件。
- 确认方式：读取实物铭牌/订单 SKU；断电测量线圈电阻并由额定值复核；确认后再选电源、MOSFET、续流和保护方案。
- 是否涉及硬件风险：高。
- 是否阻止当前开发：阻止驱动电气设计、供电选择和任何通电测试；不阻止安全状态机和 Mock。
- 优先级：最高。

## V-007 机械臂坐标方向、可达区和项目安全范围

- 问题：当前安装姿态下的坐标方向、零位、夹爪方向、碰撞边界和安全速度/范围是什么？
- 已搜索范围：NexArm 运动控制教程、外部主控 SDK、现有 `TaskSuite_E` 标定配置和机械臂源码。
- 当前候选：源码给出数据编码和既有工程数值，但这些不能证明新安装的物理单位、方向或安全边界。
- 无法确认原因：取决于实机装配、末端负载、工作台和标定。
- 确认方式：先建立 Mock 与软件限位；经用户批准后以低速、无负载、急停可用的分阶段标定确认。
- 是否涉及硬件风险：高。
- 是否阻止当前开发：阻止真实运动与坐标硬编码；不阻止算法、规划和仿真。
- 优先级：最高。

## V-008 当前相机、设备节点、成像方向和标定参数

- 问题：实际相机型号、接口、V4L2 节点/格式、固定方式、画面方向和相机到工作平面的标定是什么？
- 已搜索范围：NexArm AI 视觉教程、K230/WonderMK 资料、`TaskSuite_E/k230/`、板端 USB/基础资料及索引中的 V4L2/GStreamer/OpenCV 关键词。
- 当前候选：现有材料主要描述 K230/WonderMK 或厂商示例，未形成当前 Jetson 相机配置事实。
- 无法确认原因：设备枚举、安装位姿和内外参必须由当前实机与图像测量确定。
- 确认方式：核对相机铭牌/接口；经批准后只读枚举视频设备和格式，采集静态标定图完成标定。
- 是否涉及硬件风险：低。
- 是否阻止当前开发：阻止真实采集参数和像素到机械坐标映射；不阻止离线图像算法。
- 优先级：高。

## V-009 Q1 腕部 roll 与纸面内旋转的实机标定

- 问题：`arm_calibration` 中的 `wrist_roll_zero_deg`、`wrist_roll_sign`、`wrist_roll_min_deg`、`wrist_roll_max_deg` 是否与当前安装姿态下 NexArm `roll` 的纸面内旋转一致？
- 已搜索范围：`docs/NexArm机械臂/.../UART_Control/nexarm_sdk.py` 的 `set_pose`；`2026E/q1/calibration.py`；用户确认 roll 为纸面内旋转。
- 当前候选：工程决策 D-006 将刚性对齐角映射到 roll；零位与方向待实机低速验证。
- 无法确认原因：末端安装、夹爪朝向与坐标系定义取决于实机装配，厂商 SDK 未在已读资料中给出 roll 与纸面旋转的一一证明。
- 确认方式：经用户批准后低速、无负载、小角度（如 ±15°）roll 指令，对照纸面标定板或已知矩形碎片旋转方向，记录并写入 `arm_calibration` JSON。
- 是否涉及硬件风险：中至高。
- 是否阻止当前开发：阻止带旋转的真实拼接；不阻止离线几何与视觉算法。
- 优先级：高。

## V-010 Q1 电磁铁吸取点、工具半径与释放侧移

- 问题：真实吸取点相对碎片几何中心的偏移、工具有效半径、释放剥离方向/距离、安全工作区是否与当前评分默认启发式一致？
- 已搜索范围：`docs/interfaces/magnet_control/`；`2026E/q1/config.py` 中 `TOOL_RADIUS_MM`/`RELEASE_PEEL_DISTANCE_MM`；`runtime_config.release_peel_delta`。
- 当前候选：离线默认以几何中心为 `pick_point_source_mm`；工具半径 8 mm、余量 4 mm 仅为选择器启发式，非实机标定。
- 无法确认原因：电磁铁安装位姿与剥离策略只能由实机测量确认。
- 确认方式：批准后在观察位用已知碎片测吸取偏心；测量侧移安全方向并写入 runtime/arm 标定。
- 是否涉及硬件风险：中至高。
- 是否阻止当前开发：阻止高置信真实释放路径；不阻止离线规划与视觉。
- 优先级：高。

## V-011 Jetson 当前 NexArm 端点与安装后复位安全

- 问题：HOME XYZ `(173,4,226)` 已验证可达且旋转后的 A4 完整入画，但实际 pitch=-83.7° 时图像存在约 10% 的上下对称梯形；pitch=-90° 是否可达并改善透视尚未验证。
- 已搜索范围：Jetson 运行报告 `20260729_122043_166630`、`20260729_123051_357485`、`20260729_124259_493009`、`20260729_125749_401725`；对应 `home.jpg`；NexArm UART SDK 与厂商示例。
- 当前候选：稳定端点为 `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`；HOME/正式观察位为 `(173,4,226,-90,0,0)`；复位测试范围为 `x[150,200]`、`y[-20,20]`、`z[200,250]`、`pitch[-95,-80]`，HOME 不贴边；位置容差保持 7 mm。
- 无法确认原因：机械臂坐标 pitch 与相机光轴可能存在安装偏角，单凭末端坐标不能证明相机与桌面平行。
- 确认方式：同步后保持工作空间清空、急停可用并低速复测。比较新 `home.jpg` 的上下边宽度差；若明显缩小则保留 -90°，若不改善或不可达则恢复可达 pitch 并改支架/做透视标定。
- 是否涉及硬件风险：高。
- 是否阻止当前开发：不阻止离线开发；完成该验证前阻止完整 Q1 真实抓放。
- 优先级：最高。

## V-012 Jetson 系统时间与磁盘余量

- 问题：2026-07-30 核查时，Jetson 与控制端都标记 `+08:00`，但 Jetson 时间约慢 9 小时 20 分；根分区使用率为 91%，仅余约 16 GiB。
- 已搜索范围：Jetson `date -Ins`、控制端 `Get-Date`、Jetson `df -h /home/jetson/2026E`。
- 当前候选：系统时钟/NTP 同步异常；磁盘主要占用尚未做范围分析。
- 无法确认原因：本轮没有获准修改系统时间、NTP 或清理非项目数据。
- 确认方式：现场确认网络和 NTP 配置后校时；另行只读统计大目录，经过用户确认后只清理可重建缓存。校时前以报告内容和 SHA256 关联产物，不只依赖时间戳。
- 是否涉及硬件风险：低；错误时间会影响运行排序和日志关联，磁盘满会导致抓图或报告写入失败。
- 是否阻止当前开发：不阻止本次无运动预检；正式连续运行前应处理。
- 优先级：中。
