# 必须由实机或现场信息确认的事项

以下事项已经搜索原始 PDF、源码、ZIP 清单和相关图页；不是“尚未搜索”的占位项。

## V-001 当前 Jetson 模组与载板的精确型号

- 问题：当前实物是何种 Jetson Orin 模组、官方载板还是亚博/SUB 载板，是否属于 SUPER 配置？
- 已搜索范围：`docs/板端/01 第一章 快速上手/`、`docs/板端/02 第二章 主板基础/`、`docs/板端/04 第四章 GPIO控制/`，并视觉核查 `主板介绍.pdf` 第 1 页和 `GPIO说明.pdf` 第 2 页。
- 当前候选：资料同时覆盖 Jetson Orin 官方套件、SUB 套件和 SUPER 相关流程。
- 无法确认原因：资料集合描述多个产品/流程，没有当前实物铭牌或系统身份输出。
- 确认方式：检查模组和载板丝印/订单；在获准后记录只读系统信息（如 JetPack/L4T、设备树 model），再与对应厂商资料匹配。
- 是否涉及硬件风险：低；错误结论用于 GPIO/刷机时风险高。
- 是否阻止当前开发：阻止板级 GPIO、设备树和系统镜像相关实现；不阻止纯 Mock 软件设计。
- 优先级：高。

## V-017 NexArm 静止末端机械抖动定位

- 当前证据：只读诊断 `20260730_233809_392899` 中控制板电压约 12.419 V；未发送运动或电磁铁命令；20 次真实关节查询的六轴脉冲跨度均为 0。静止抖动不是 Q1 上位机高频重复下发造成的。
- 尚未确认：舵机内部位置环是否振荡、末端支架或舵盘是否松动、线缆是否周期性牵拉、实际静止位姿是否接触桌面，以及各舵机温度/电流/负载。直读舵机寄存器仍全部超时。
- 下一步：断电后检查末端支架、舵盘中心螺丝、连接螺丝和线缆余量；重新上电后确认抖动来自具体舵机还是松动结构。任何扭矩、PID、死区、加速度、偏差或模式写入，以及改变 HOME/BUFFER 姿态的运动对照，均需用户另行明确批准。
- 是否阻止当前开发：阻止继续带末端负载的完整 Q1 实机运行；不阻止视觉与离线规划。
- 优先级：最高。

## V-002 当前 JetPack/L4T 与 GPIO 配置状态

- 问题：实机 JetPack、L4T、内核、GPIO 库和 pinmux/设备树的实际版本与状态是什么？
- 已搜索范围：`docs/板端/02 第二章 主板基础/` 和 `docs/板端/04 第四章 GPIO控制/` 全部 PDF 的逐页索引，相关 GPIO 图页已视觉检查。
- 当前候选：资料包含多个系统烧录、升级和 SUPER 修复流程。
- 无法确认原因：软件版本和 pinmux 是实机可变状态，教程版本不能证明当前状态。
- 确认方式：用户批准后执行只读版本/设备树检查；任何 Jetson-IO、`devmem` 或写入操作需另行明确批准。
- 是否涉及硬件风险：只读检查低，配置写入高。
- 是否阻止当前开发：阻止真实 GPIO 映射和驱动部署；不阻止接口抽象与 Mock。
- 优先级：高。

## V-003 当前 NexArm 控制板和固件版本

- 问题：当前机械臂控制板硬件版本、角色和实机固件版本是什么？
- 已搜索范围：NexArm 快速入门、外部主控二次开发 PDF/SDK、`SCH_NexArm V1.0.pdf` 图页、出厂固件目录和 ZIP 清单。
- 当前候选：V1.0 NexArm-ESP32 控制器图纸；资料包包含 follower V1.2 和 leader V1.1 固件文件。
- 无法确认原因：图纸/固件包的存在不证明实机硬件或当前已烧录版本。
- 确认方式：先看控制板丝印/标签；经用户明确批准并确保不运动后，使用 SDK 的只读固件查询或厂商工具读取。
- 是否涉及硬件风险：中；连接或误发命令可能触发机械动作。
- 是否阻止当前开发：阻止真实连接、固件选择和运动测试；不阻止协议静态分析。
- 优先级：高。

## V-004 NexArm 当前通信链路和端点

- 问题：项目最终使用 UART 还是 Wi-Fi，实际串口节点或主机地址/端口是什么？
- 已搜索范围：外部主控二次开发 PDF、UART/Wi-Fi SDK 与 Demo、ZIP 内部路径、Jetson 远程连接教程。
- 当前候选：SDK 同时提供 UART `NexArmClient` 和 Wi-Fi `NexArmWiFiClient`；源码中存在示例默认值。
- 无法确认原因：端点取决于当前接线、系统枚举、网络配置和项目主动选择。
- 确认方式：先作架构决策；经批准后只读枚举串口/网络，再进行不运动的握手验证。
- 是否涉及硬件风险：中。
- 是否阻止当前开发：阻止真实传输适配器启用；不阻止统一接口和 Mock。
- 优先级：高。

## V-005 当前 MOSFET 模块的输入极性与 3.3 V 兼容性

- 问题：实际 15 A MOSFET 模块是否确为高电平开启、低电平关闭，当前控制脚 PC0 的 3.3 V 高电平是否能可靠驱动，输入端是否自带上拉/下拉或反相级？
- 已搜索范围：docs/MOSFET驱动15A资料包/ 的产品资料、接线图片、ZIP 目录和 STM32 示例源码；示例只确认用 PC13 输出控制，没有形成当前模块输入电路和极性的可靠厂商结论。
- 当前候选：固件按高开、低关实现；VET6 控制脚已改为 PC0（D-038）。2026-07-31 Jetson MAGNET_ON 100 后固件状态按时关闭，用户已实际接电磁铁测试；但仍缺独立的模块输入电路/极性测量记录。
- 无法确认原因：输入电路、默认状态和极性取决于手中模块版本，必须通过板上丝印/原理图或空载测量确认；一次吸合成功不能替代输入电气规格。
- 确认方式：断电核对模块型号与输入原理图；空载测量 PC0/MOSFET 控制端默认电平与高/低响应。
- 是否涉及硬件风险：高；极性或电路判断错误可能导致上电自动吸合。
- 是否阻止当前开发：不阻止固件与串口协议开发；正式长时间带载与机械臂联动前仍应补齐极性/电平测量。
- 优先级：最高。

## V-006 电磁铁精确型号、额定电压和线圈参数

- 问题：实际使用 P20/15、ZHI-1727 或其他型号，额定电压、电阻、电流和允许通电周期是多少？
- 已搜索范围：`docs/MOSFET驱动15A资料包/资料/` 两份产品手册、`接线方式/` 图片、三个示例 ZIP 目录与源码。
- 当前候选：P20/15 和 ZHI-1727。`P20_15_5V电磁铁产品手册.pdf` 文件名含“5V”，但第 1 页产品照片铭牌可见“DC12V”，形成明确冲突。
- 无法确认原因：资料包包含多型号且文件名与图页不一致，不能确定到手器件。
- 确认方式：读取实物铭牌/订单 SKU；断电测量线圈电阻并由额定值复核；确认后再选电源、MOSFET、续流和保护方案。
- 是否涉及硬件风险：高。
- 是否阻止当前开发：阻止驱动电气设计、供电选择和任何通电测试；不阻止安全状态机和 Mock。
- 优先级：最高。

## V-007 机械臂坐标方向、可达区和项目安全范围

- 问题：当前安装姿态下的坐标方向、零位、夹爪方向、碰撞边界和安全速度/范围是什么？
- 已搜索范围：NexArm 运动控制教程、外部主控 SDK、现有 `TaskSuite_E` 标定配置和机械臂源码。
- 当前候选：源码给出数据编码和既有工程数值，但这些不能证明新安装的物理单位、方向或安全边界。
- 无法确认原因：取决于实机装配、末端负载、工作台和标定。
- 确认方式：先建立 Mock 与软件限位；经用户批准后以低速、无负载、急停可用的分阶段标定确认。
- 是否涉及硬件风险：高。
- 是否阻止当前开发：阻止真实运动与坐标硬编码；不阻止算法、规划和仿真。
- 优先级：最高。

## V-008 当前相机、设备节点、成像方向和纸面检测稳定性

- 问题：K230 当前固定端点与分辨率已基本明确；正式路径已改为每帧 `detect_paper`，不再使用静态纸面四角 JSON。当前安装姿态下检测是否稳定？
- 已搜索范围：NexArm AI 视觉教程、K230/WonderMK 资料、`TaskSuite_E/k230/`、`q1/vision.py::detect_paper`、用户 HOME 构图。
- 当前候选：相机口 `/dev/serial/by-id/usb-1a86_USB_Single_Serial_5B7A028646-if00`，1280x720@460800；A4 四角由实图检测。
- 无法确认原因：横放 A4、轻微透视和光照变化下的自动角点质量必须由当前实机图像验证。
- 确认方式：在统一 HOME 下连续采集，检查 `detect_paper` 四角与分界线是否稳定，并核对碎片纸面坐标。
- 是否涉及硬件风险：低。
- 是否阻止当前开发：不阻止软件闭环开发；检测失败会阻止该轮抓放。
- 优先级：高。

## V-009 Q1 腕部 roll 与纸面内旋转的实机标定

- 问题：`arm_calibration` 中的 `wrist_roll_zero_deg`、`wrist_roll_sign`、`wrist_roll_min_deg`、`wrist_roll_max_deg` 是否与当前安装姿态下 NexArm `roll` 的纸面内旋转一致？
- 已搜索范围：`docs/NexArm机械臂/.../UART_Control/nexarm_sdk.py` 的 `set_pose`；`2026E/q1/calibration.py`；用户确认 roll 为纸面内旋转。
- 当前候选：工程决策 D-006 将刚性对齐角映射到 roll；零位与方向待实机低速验证。
- 无法确认原因：末端安装、夹爪朝向与坐标系定义取决于实机装配，厂商 SDK 未在已读资料中给出 roll 与纸面旋转的一一证明。
- 确认方式：经用户批准后低速、无负载、小角度（如 ±15°）roll 指令，对照纸面标定板或已知矩形碎片旋转方向，记录并写入 `arm_calibration` JSON。
- 是否涉及硬件风险：中至高。
- 是否阻止当前开发：阻止带旋转的真实拼接；不阻止离线几何与视觉算法。
- 优先级：高。

## V-010 Q1 电磁铁吸取点、工具半径与释放侧移

- 问题：真实吸取点相对碎片几何中心的偏移、工具有效半径、释放剥离方向/距离、安全工作区是否与当前评分默认启发式一致？
- 已搜索范围：`docs/interfaces/magnet_control/`；`2026E/q1/config.py` 中 `TOOL_RADIUS_MM`/`RELEASE_PEEL_DISTANCE_MM`；`runtime_config.release_peel_delta`。
- 当前候选：离线默认以几何中心为 `pick_point_source_mm`；工具半径 8 mm、余量 4 mm 仅为选择器启发式，非实机标定。
- 软件接入状态：真实 `stm32` 后端已按 D-026 接入源点到位后 ON、搬运续租、目标到位后 OFF；离线 Mock 只能证明命令顺序与异常关闭，尚未替代线圈通电、吸力及释放实测。
- 无法确认原因：电磁铁安装位姿与剥离策略只能由实机测量确认。
- 确认方式：批准后在观察位用已知碎片测吸取偏心；测量侧移安全方向并写入 runtime/arm 标定。
- 是否涉及硬件风险：中至高。
- 是否阻止当前开发：阻止高置信真实释放路径；不阻止离线规划与视觉。
- 优先级：高。

## V-011 Jetson 当前 NexArm 端点与安装后复位安全

- 问题：HOME `(173,4,226,-84.4,0,0)` 已验证可达，但用户现场反馈 A4 经常拍不全；提高到 Z241 的实测未到位并已回退。原位实际 pitch=-83.7° 时图像存在约 10% 的上下对称梯形；后续 pitch=-90° 复测没有发生可确认运动。
- 已搜索范围：Jetson 运行报告 `20260729_122043_166630`、`20260729_123051_357485`、`20260729_124259_493009`、`20260729_125749_401725`；对应 `home.jpg`；NexArm UART SDK 与厂商示例。
- 当前候选：稳定端点为 `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`；HOME/正式观察位恢复为 `(173,4,226,-84.4,0,0)`；正式工作区 Z 上限为 260 mm，不再配置或发送独立安全高度航点，位置容差为 10 mm。
- 无法确认原因：机械臂坐标 pitch 与相机光轴可能存在安装偏角，单凭末端坐标不能证明相机与桌面平行。
- 确认方式：不再重试 -90 或 Z241；保持已验证 HOME，调整纸张位置或相机支架后检查 A4 四边是否完整入画及 `detect_paper` 稳定性。
- 是否涉及硬件风险：高。
- 是否阻止当前开发：不阻止离线开发；完成该验证前阻止完整 Q1 真实抓放。
- 优先级：最高。

## V-012 Jetson 系统时间与磁盘余量

- 问题：2026-07-30 核查时，Jetson 与控制端都标记 `+08:00`，但 Jetson 时间约慢 9 小时 20 分；根分区使用率为 91%，仅余约 16 GiB。
- 已搜索范围：Jetson `date -Ins`、控制端 `Get-Date`、Jetson `df -h /home/jetson/2026E`。
- 当前候选：系统时钟/NTP 同步异常；磁盘主要占用尚未做范围分析。
- 无法确认原因：本轮没有获准修改系统时间、NTP 或清理非项目数据。
- 确认方式：现场确认网络和 NTP 配置后校时；另行只读统计大目录，经过用户确认后只清理可重建缓存。校时前以报告内容和 SHA256 关联产物，不只依赖时间戳。
- 是否涉及硬件风险：低；错误时间会影响运行排序和日志关联，磁盘满会导致抓图或报告写入失败。
- 是否阻止当前开发：不阻止本次无运动预检；正式连续运行前应处理。
- 优先级：中。

## V-013 Q1 低高度完整抓放路径（已完成）

- 当前结论：正式配置包含 `pick_height=-15`、`release_height=-15` 和 `buffer_pose=[175,0,80,-90,0,0,3000]`。D-042 已将低位直达搬运改为固定 BUFFER 编排，最后一片释放后返回 HOME；不恢复历史纯升降或固定 Z 航点。新轨迹尚未实机验证。
- 新增实测：`20260729_134450_208600`、`20260729_134517_829094` 的视觉、审计、选择和P3规划均成功，但第一条原地 Z250 命令连续两次超时，最终反馈均为 `(168,5,219,-86.9,0,0)`，未发送横移、下降或磁铁命令。用户确认 Z250 超出当前安全范围且该原地预抬升不需要；曾临时改为 Z226 转运，现已继续由 D-022 的完整六维直达模式覆盖。
- 新增实测：`20260729_135642_047211`、`20260729_135719_628157` 的视觉与规划也成功，但源 XY/Z226 中间点分别在约 12 秒后超时；每次约 47 个反馈样本均无变化，说明该中间位姿未被实际执行。两次刚体规划最大顶点残差约 14.65 mm 和 18.13 mm，释放精度仍需改善，不能把识别成功等同为精确拼放已验证。
- 历史实测：改为完整六维直达后，`20260729_141607_088180` 和 `20260729_141721_922896` 的源目标分别为 `(247.997,37.906,25,-84.4,0,0)`、`(244.745,32.969,25,-84.4,0,0)`；两次反馈仍全程固定在 `(168,5,219,-86.9,0,0)`。这排除了旧 Z226 航点是唯一原因，也不能靠放宽 10 mm 容差或延长 12 秒超时解决。该记录解释历史门禁，不覆盖后续 Z15 实机确认。
- 独立软件问题：两次 P3 刚体拟合最大/RMS残差分别约 `18.34/14.83 mm`、`18.06/15.02 mm`，超过 8 mm。现已增加硬门禁；这解决错误规划进入运动的问题，但不解决轮廓或模板几何本身的误差。
- 几何根因已定位：四块实物相对题图目标全部为一致镜像手性，而不是四块随机检测失真。按 D-024 对整套纯色目标布局做 `mirror_x` 后，两次 P3 最大残差降为 2.265 mm、2.394 mm，规划门禁可通过。该结果不能解除下方机械位姿门禁。
- 历史实测：首次 `RUN_Q1_ARM` 在只发送 HOME 后超时，未发送任何抓放轨迹；随后只读反馈为约 `(24,1,144,-135.8,0,0)`，与 HOME 明显不符。生产执行器后来曾补齐低全局加速度写入；该写入已由 D-035 删除，当前入口直接发送 HOME。
- 修复后 `RUN_Q1_HOME` 从上述远端位姿运动，最终稳定反馈为 `(168,5,219,-86.7,0,0)`；相对目标的位置合成误差为 `8.66 mm`，超过固定 7 mm，姿态误差 2.3°在 3°内。肉眼看似复位不能替代到位门限；不得静默放宽。
- 已搜索范围：现统一为 `2026E/q1/config/robot_config.json`；`q1/HANDOFF.md` 中的 Z=150 实测记录；NexArm 执行器和正式逐片规划。
- 当前配置：用户批准的 XY 矩阵继续作为工程输入；D-036 已将 HOME 定义为 `(175,0,210,-90,0,0)`；D-033 记录 Z15 完整吸取和释放位姿已到位。
- 剩余边界：该确认不单独证明磁吸可靠性、碎片搬运保持或最终拼放精度，相关状态继续由 `physical_pick_verified` 和动作后结果记录表达。
- 确认结果：用户于 2026-07-30 明确确认完整吸取、释放位姿均已到位，配置已将 `direct_pick_release_pose_verified` 置 true。
- 独立单目标脚本 `calibrate_single_pose` 已删除。保留证据：`q1_pose_calibration/20260729_153642_988359`（物理运动 vs 陈旧 HOME 反馈）。D-037 后生产路径按每条位姿的配置动作时长推进，不再依赖这组陈旧反馈；源位姿时长结束后磁铁 ON，目标位姿时长结束后磁铁 OFF。
- 2026-07-30 无运动诊断：`python3 -m q1.scripts.diag_feedback_freshness_no_motion` 在固件 1.0.0 上连续 3 次 flush+查询均返回带 `request_started_s`/`response_received_s` 的坐标回复（延迟约 2–25 ms）；两次查询间隙仍观察到 20/30 字节积压被丢弃，说明清缓冲有必要。未发送任何 `set_pose`。受控位姿闭环仍待现场单独确认。
- 2026-07-30 HOME 复测与软件审计见 F-023/F-024/F-026：旧 HOME 稳定停在 `(168,5,215,-88,1,1)`；运行 `20260730_231532_316942` 又从 `(500,0,73,0,0,0)` 向 `(180,0,200,-90,0,0)` 发送单条 HOME 后坐标和舵机仍零变化。当前 HOME 已按 D-036 改为 `(175,0,210,-90,0,0)`；抓放位姿已按 D-033 独立确认。
- 2026-07-30 主流程按 D-030/D-037 收缩为 HOME 时长结束后单次观察、生成完整 `PieceMove` 队列并立即执行；当前生产配置不再触发 `DIRECT_PICK_RELEASE_POSE_UNVERIFIED`。
- 是否涉及硬件风险：最高。
- 是否阻止当前开发：否；本项位姿门禁已经解除。
- 优先级：最高。

## V-014 当前观察位的 A4 全幅识别与四片规划（已完成）

- 问题：当前机械臂固定观察位下，K230 的 1280×720 实图能否完整包含 A4，并稳定一一识别 P1–P4，生成四片后续机械位姿规划？
- 已搜索范围：`q1/vision.py::detect_paper`、`q1/analyzer.py::SceneAnalyzer`、`q1/motion.py::plan_piece_moves`、K230 TTL Jetson 驱动和既有横放 A4 回归图。
- 当前状态：运行 `20260730_232815_277491` 的 K230 `home_timeout.jpg` 包含完整 A4 和四块碎片；当前代码离线复算得到 `paper_valid=true`、`scene_valid=true`、4 块碎片及按 P1–P4 排序的 4 条规划，成功生成 `capture.png`、`plan.png`、`scene.json`、`piece_moves.json`。
- 确认结果：本项视觉构图与规划已由实机原图确认；修改后的完整 `q1.main run` 是否能连续完成机械动作和磁吸仍属独立实机验证。
- 适用边界：本项只确认构图、视觉与规划，不证明机械臂可达、吸取点准确、电磁铁有效或拼放精度达标。
- 是否涉及硬件风险：低，只访问 K230 TTL 串口；不初始化 NexArm 或 STM32。
- 是否阻止当前开发：否。
- 优先级：最高。

## V-015 当前固定四片 1.03 目标比例实机复核

- 问题：`target_scale=1.03` 生成的约 103×61.8 mm 目标能否比原 100×60 mm 目标更贴合当前四片的实际拼接边界？
- 已搜索范围：赛题第 2 页图 2；运行 `20260731_012300_357937` 的原图、场景 JSON、规划 JSON；当前模板和刚体规划源码。
- 当前状态：同一实图离线复算后四片最大刚体残差均不超过 1.977 mm；目标原点已改为 `(53.5,191.85) mm`，使缩放外框位于右侧识别区域中央；尚未部署或执行新的释放坐标。
- 确认方式：同步本地 Q1 代码和配置后完成一次 Q1，比较实物相邻顶点与外包框；若仍有统一尺寸偏差，只微调 `target_scale`，不逐片改模板。
- 是否涉及硬件风险：高，验证会执行机械臂和电磁铁。
- 是否阻止当前开发：否；阻止把 1.03 称为实机拼放已验证。
- 优先级：高。

## V-016 A4 上下边未拍全时的正式单帧识别

- 问题：真实 K230 帧中 A4 上下边轻微超出画面、四片和左右纸边仍完整时，正式主流程能否稳定生成四片规划？
- 当前状态：三张 1024×767/768 特殊样本已按“左右黑色半区 + 中央白线 + A4 比例外推”离线复算通过，均为 `paper_valid=true`、`scene_valid=true` 且 P1–P4 齐全；尚未同步 Jetson。
- 确认方式：部署后只需检查一次正式 `capture.png`、`scene.json` 和 `plan.png`；应为 `paper_valid=true`、`scene_valid=true`、P1–P4 齐全，规划目标仍位于右侧区域中央。
- 是否涉及硬件风险：正式完整运行为高；只运行 `plan` 为低。
- 是否阻止当前开发：否。
- 优先级：高。
